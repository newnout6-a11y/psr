# Kwork Market Recommendation Journal

Дата: 2026-07-10

## Решение

- Блок рекомендаций переименован в `Что искать сейчас`.
- Рекомендации строятся по текущей рубрике, а не по фиксированным `*_low_offer` окнам.
- Внутренние control windows остаются только как скрытая диагностика.

## Что изменено

- Обновлён бриф `docs/kwork_market_recommendation_brief_20260709.md`.
- В `src/platforms/kwork_market.py` добавлен AI-путь для рубрикаторных рекомендаций.
- В `src/api/routes/kwork.py` и `desktop/src/lib/api.ts` добавлены поля контекста рубрики.
- В `desktop/src/pages/KworkMarket.tsx` блок рекомендаций переведён на новые данные и добавлен показ контрольных окон по кнопке.

## Канон

- В AI передаются сырые данные только текущей рубрики.
- AI возвращает запросы с `count`, `priority`, `why`.
- `*_low_offer` не показываются как совет пользователю.
